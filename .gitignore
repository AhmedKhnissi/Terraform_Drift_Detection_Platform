# Build artifacts
/driftdetect
driftdetect.exe

# Local state / storage
*.db
terraform.tfstate
terraform.tfstate.backup

# Local config with secrets
config.yaml

# Terraform working directory (downloaded providers, including large binaries)
.terraform/
